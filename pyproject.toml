[project]
name = "phone-scrapper-v2"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.57.0",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "structlog>=25.5.0",
    "tenacity>=9.1.2",
]
